class Solution {
    public boolean stoneGame(int[] piles) {
        // Alice can always force a win by picking all piles at even indices
        // or all at odd indices. Since the total sum is odd, one group is larger.
        return true;
    }
}